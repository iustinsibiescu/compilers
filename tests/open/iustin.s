main {
	
  s1:int := sum(-10_1/2,20);
  s2:float := sum(10.0,-20.0);
  b:bool;
   a : seq<top> := [isd[1:v],[]];

	f(a);
	#
#
  if (s in cacat[2]) then
     b  :=  s1 + s2 / (s1 + s2) <= 30;
	fi

	forall (s in cacat[2:6]) do
		 b  :=  s1 + s2 / (s1 + s2) <= 30;
	od
	/#read a;
	#print a;#/
#
#
#
#

	
};



