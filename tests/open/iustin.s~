l : float := 3;
l : float := 3;

fdef sum(i:int, j:int) {
asd:dict<int,chaa>;
	a := 2+{1:2};

     return 2;
} : int;

a : int;

fdef sum(i:float, j:float) {
	v:= T;
     return i + j;
} : float;

main {
	
  s1:int := sum(-10,20);
  s2:float := sum(10.0,-20.0);
  b:bool;
	a : seq<int> := [1,2,3,[1,2,3]];	
  if (s1 < s2 || s1 = s2) then
     b  :=  s1 + s2 / (s1 + s2) <= 30;
	fi
	
}


