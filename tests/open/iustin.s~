fdef fibonacci( pos : int ) { 
	if (pos = -1) then
		#return 0;
		a:=a;
	fi
	if (pos = 0) then
		return 1;
		#asd := asf;
	fi	
	return fibonacci(pos-1) + fibonacci(pos-2);
};

m#ain {
	
	alias seq<char> string;
	alias fred spud;
	tdef person { name:int, jk:string, sadfsadf:safdasdf };
	a : string := "ss";
	a : dict<int,sads> := {1:"Sasdsaniud"::"AS"::"Asfa", 2:{}, 1:ds[1:2] };
	fnewlist:seq<tosp> := "saf";
	f := asfas :: asfas;
	a := f^fF(a);
	return;
};



